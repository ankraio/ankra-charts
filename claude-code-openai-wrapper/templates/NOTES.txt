{{ include "claude-code-openai-wrapper.fullname" . }} ({{ .Chart.Name }}-{{ .Chart.Version }}, upstream {{ .Chart.AppVersion }})

Image:
  {{ include "claude-code-openai-wrapper.image" . }}

Watch it come up:

  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "claude-code-openai-wrapper.fullname" . }}
  kubectl -n {{ .Release.Namespace }} logs -f deploy/{{ include "claude-code-openai-wrapper.fullname" . }}

Point an OpenAI client at it:

{{- if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  base_url: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/v1
{{- end }}
{{- else if .Values.service.enabled }}
  base_url: http://{{ include "claude-code-openai-wrapper.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}/v1
  (in-cluster; use kubectl port-forward svc/{{ include "claude-code-openai-wrapper.fullname" . }} {{ .Values.service.port }} to try it locally)
{{- end }}

Secrets:
{{- if .Values.externalSecret.enabled }}
  Delivered by the External Secrets Operator into {{ include "claude-code-openai-wrapper.secretName" . }}.
{{- else if .Values.secrets.existingSecret }}
  Read from the existing Secret {{ .Values.secrets.existingSecret }}.
{{- else if include "claude-code-openai-wrapper.hasInlineSecrets" . }}
  WARNING: rendered from inline values into {{ include "claude-code-openai-wrapper.secretName" . }}.
  They are readable in the Helm release Secret and in whatever holds your values
  file. Move them to secrets.existingSecret or externalSecret.enabled.
{{- end }}
{{- if and (gt (int .Values.replicaCount) 1) (ne .Values.service.sessionAffinity "ClientIP") }}

NOTE: {{ .Values.replicaCount }} replicas with sessionAffinity {{ .Values.service.sessionAffinity }}.
  Sessions (/v1/sessions, session_id continuity) live in each pod's memory, so
  a follow-up request can land on a pod that has never seen the conversation.
  Stateless chat completions are unaffected; for session continuity set
  service.sessionAffinity=ClientIP or run one replica.
{{- end }}

Network:
{{- if .Values.networkPolicy.enabled }}
  A NetworkPolicy restricts this pod to DNS{{ if .Values.networkPolicy.egress.allowPublicInternet }} and outbound {{ range $index, $port := .Values.networkPolicy.egress.ports }}{{ if $index }}, {{ end }}{{ $port.protocol }}/{{ $port.port }}{{ end }}, with these
  destinations excluded: {{ join ", " .Values.networkPolicy.egress.excludedCidrs }}{{ end }}.
  This is only enforced if your CNI implements NetworkPolicy (Cilium, Calico,
  Antrea, ...). On a CNI that ignores it, the policy is decoration - check with:

    kubectl -n {{ .Release.Namespace }} describe networkpolicy {{ include "claude-code-openai-wrapper.fullname" . }}
{{- else }}
  WARNING: networkPolicy.enabled=false. The wrapper's tool mode can execute
  commands in this pod, with unrestricted egress including to the cloud
  metadata endpoint.
{{- end }}
{{- if not .Values.hardening.enabled }}

WARNING: hardening.enabled=false - every render-time guardrail in this chart is
switched off. Do not run this configuration in a shared or production cluster.
{{- end }}
