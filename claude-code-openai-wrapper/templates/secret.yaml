{{- if and (not .Values.externalSecret.enabled) (not .Values.secrets.existingSecret) }}
{{- $inlineSecretKeys := include "claude-code-openai-wrapper.inlineSecretKeys" . | fromJsonArray -}}
{{- if gt (len $inlineSecretKeys) 0 }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "claude-code-openai-wrapper.secretName" . }}
  labels:
    {{- include "claude-code-openai-wrapper.labels" . | nindent 4 }}
  {{- with .Values.secrets.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
  {{- range $key := $inlineSecretKeys }}
  {{ $key }}: {{ index $.Values.secrets $key | toString | quote }}
  {{- end }}
{{- end }}
{{- end }}
